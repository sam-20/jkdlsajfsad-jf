import React from 'react'

function Arrays() {

    let b = ["mary", "alfred", { car: "nissan", model: "sentra" }, 9, true]
    let x = { name: "daniel", age: 14, location: "kumasi" }
    let y = [67, "mango", true, "jane"]
    let z = [{ maths: 24, english: 56 }, { fruit: "orange", cost: 56.3 }]
    let c = [{}]
    var e = ["samuel", "book", "phone", 45, false, "pc"]
    var a = "akljfdsf"
    var f = false
    var num = 0.5
    var d = { name: "smaklfa" }

    let indexofbook = e.indexOf("book")
    let indexofnum = e.indexOf(45)
    // e.splice(indexofbook,1)
    // e.pop(e.indexOf("book"))
    // console.log(e.splice(e.indexOf("book"), 1))
    // console.log(e);
    // console.log(c.length);

    // e.splice(2, 2)
    // console.log(e);

    // console.log(b[1]);
    // console.log(z[1]);
    // console.log(z[1].cost);
    // console.log(c[0]);
    // console.log(b[1].model);
    // console.log(x.location);
    // console.log(b[2].model);

    // e.unshift({ score: 56, name: "king" })
    // console.log(e);
    // e.unshift(67)
    // e.shift()
    // console.log(e);

    // z.push("samsung")
    // console.log(z);
    // z.pop()
    // console.log(z)

    // b.splice(2,2)
    // console.log(b);

    // e.splice(3,1)
    // console.log(e)


    return (
        <div>

        </div>
    )
}

export default Arrays
